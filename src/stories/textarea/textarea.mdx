import { Meta, Canvas, Controls } from "@storybook/addon-docs/blocks";
import * as TextareaStories from "./textarea.stories";

<Meta of={TextareaStories} />

# dsh-textarea

Web component de área de texto del DSH Design System. Construido con Atomico sobre Shadow DOM.

Proporciona un campo de texto multilínea con contador de caracteres, mensaje de ayuda, estados visuales de error/éxito, redimensionamiento automático y control de longitud máxima. Emite el evento `changeTextarea` con el contenido actual al escribir.

## Default

<Canvas of={TextareaStories.Default} />

## Con contador

La prop `counter` activa el contador de caracteres restantes. Usarlo junto a `maxlength` para mostrar el límite.

<Canvas of={TextareaStories.ConContador} />

## Con helper message

<Canvas of={TextareaStories.ConHelper} />

## Resize automático

Con `resize-height` activo, el textarea crece verticalmente según el contenido ingresado, eliminando la necesidad de scroll.

<Canvas of={TextareaStories.ResizeAutomatico} />

## Disabled

<Canvas of={TextareaStories.Disabled} />

## Error

<Canvas of={TextareaStories.Error} />

## Playground

Interactúa con todas las propiedades del textarea:

<Canvas of={TextareaStories.Playground} />
<Controls of={TextareaStories.Playground} />

## Eventos

| Evento | Detail | Descripción |
|--------|--------|-------------|
| `changeTextarea` | nuevo valor (string) | Se dispara al cambiar el contenido del textarea |

## React — Cómo usarlo

El componente `<dsh-textarea>` emite el evento `changeTextarea` con el nuevo valor como `event.detail`.

<Canvas of={TextareaStories.ReactUsage} />

El tab **Show code** muestra los dos patrones de consumo:
- **React 19** — prop `onchangetextarea` directamente en JSX
- **React 18** — `useRef` + `addEventListener('changeTextarea', handler)`
