import { Meta } from "@storybook/addon-docs/blocks";
import '@components/button';

<Meta title="Design System/Bienvenida" />

<div style={{fontFamily:"'Plus Jakarta Sans','Segoe UI',sans-serif",color:"#111827",maxWidth:"960px",margin:"0 auto",padding:"0 0 64px"}}>

{/* ─── HERO ─────────────────────────────────────────────────────────────── */}
<div style={{background:"linear-gradient(140deg,#00648f 0%,#0076a9 50%,#008ec4 100%)",borderRadius:"20px",padding:"60px 52px 52px",color:"#fff",marginBottom:"52px",position:"relative",overflow:"hidden"}}>
  <div style={{position:"absolute",top:"-80px",right:"-80px",width:"320px",height:"320px",borderRadius:"50%",background:"rgba(255,255,255,0.05)",pointerEvents:"none"}} />
  <div style={{position:"absolute",bottom:"-100px",left:"25%",width:"240px",height:"240px",borderRadius:"50%",background:"rgba(255,255,255,0.04)",pointerEvents:"none"}} />
  <div style={{position:"relative",zIndex:1}}>
    <div style={{display:"inline-flex",alignItems:"center",gap:"6px",background:"rgba(255,255,255,0.18)",backdropFilter:"blur(6px)",borderRadius:"100px",padding:"5px 14px",fontSize:"11px",fontWeight:"700",letterSpacing:"2px",textTransform:"uppercase",marginBottom:"24px",color:"#ffffff"}}>
      <span style={{width:"6px",height:"6px",borderRadius:"50%",background:"#7dd3fc",display:"inline-block"}} />
      v1.0.0 — Alfa pública
    </div>
    <h1 style={{fontSize:"48px",fontWeight:"800",margin:"0 0 16px",lineHeight:"1.1",letterSpacing:"-1px",color:"#ffffff"}}>
      DSH Design System
    </h1>
    <div style={{fontSize:"19px",fontWeight:"400",marginBottom:"36px",maxWidth:"540px",lineHeight:"1.65",color:"#ffffff"}}>
      Librería de Web Components construida con <strong>AtomicoJS</strong> sobre Shadow DOM.
      Tokens semánticos, átomos reutilizables y documentación viva en un solo lugar.
    </div>
    <div style={{display:"flex",gap:"10px",flexWrap:"wrap"}}>
      {[
        {label:"Web Components"},
        {label:"AtomicoJS"},
        {label:"Shadow DOM"},
        {label:"Design Tokens"},
        {label:"React 19+"},
      ].map(({label}) => (
        <span key={label} style={{background:"rgba(255,255,255,0.15)",border:"1px solid rgba(255,255,255,0.3)",borderRadius:"100px",padding:"6px 16px",fontSize:"13px",fontWeight:"500",color:"#ffffff"}}>
          {label}
        </span>
      ))}
    </div>
  </div>
</div>

{/* ─── MÉTRICAS ────────────────────────────────────────────────────────── */}
<div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(180px,1fr))",gap:"16px",marginBottom:"56px"}}>
  {[
    {value:"10",label:"Componentes",sub:"átomos listos"},
    {value:"200+",label:"Design Tokens",sub:"variables semánticas"},
    {value:"50+",label:"Stories",sub:"documentadas"},
    {value:"0",label:"Dependencias",sub:"externas de UI"},
  ].map(({value,label,sub}) => (
    <div key={label} style={{background:"#fff",border:"1px solid #e5e7eb",borderRadius:"16px",padding:"28px 24px",textAlign:"center",boxShadow:"0 1px 4px rgba(0,0,0,0.06)"}}>
      <div style={{fontSize:"36px",fontWeight:"800",color:"#0076a9",lineHeight:"1",letterSpacing:"-1px"}}>{value}</div>
      <div style={{fontSize:"14px",fontWeight:"700",color:"#111827",marginTop:"8px"}}>{label}</div>
      <div style={{fontSize:"12px",color:"#9ca3af",marginTop:"3px"}}>{sub}</div>
    </div>
  ))}
</div>

{/* ─── COMPONENTES ─────────────────────────────────────────────────────── */}
<div style={{marginBottom:"56px"}}>
  <h2 style={{fontSize:"24px",fontWeight:"800",color:"#111827",margin:"0 0 6px",letterSpacing:"-0.5px"}}>
    Componentes disponibles
  </h2>
  <div style={{fontSize:"15px",color:"#6b7280",margin:"0 0 24px",lineHeight:"1.5"}}>
    Todos construidos con <strong>AtomicoJS</strong>. Consumibles desde HTML vanilla, React, Vue o cualquier framework.
  </div>
  <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fill,minmax(200px,1fr))",gap:"12px"}}>
    {[
      {tag:"dsh-button",   icon:"🖱️", desc:"3 variantes · colores · loading"},
      {tag:"dsh-icon",     icon:"✨", desc:"Font Awesome · 7 tamaños"},
      {tag:"dsh-tag",      icon:"🏷️", desc:"Informativa · interactiva · news"},
      {tag:"dsh-badge",    icon:"🔔", desc:"Contador · campana · pulso"},
      {tag:"dsh-loading",  icon:"⏳", desc:"Spinner · progreso · error"},
      {tag:"dsh-checkbox", icon:"☑️", desc:"Claro · oscuro · indeterminate"},
      {tag:"dsh-radio",    icon:"🔵", desc:"Solo · grupo con teclado"},
      {tag:"dsh-input",    icon:"⌨️", desc:"Texto · password · RUT · tel"},
      {tag:"dsh-textarea", icon:"📝", desc:"Auto-resize · contador"},
      {tag:"dsh-select",   icon:"📋", desc:"Dropdown · checkbox · modal"},
    ].map(({tag,icon,desc}) => (
      <div key={tag} style={{background:"#fff",border:"1px solid #e5e7eb",borderRadius:"12px",padding:"18px 16px",boxShadow:"0 1px 3px rgba(0,0,0,0.05)"}}>
        <div style={{fontSize:"22px",marginBottom:"10px"}}>{icon}</div>
        <div style={{fontFamily:"'Fira Code','Cascadia Code',monospace",fontSize:"12px",fontWeight:"600",color:"#0076a9",marginBottom:"6px"}}>{"<"}{tag}{">"}</div>
        <div style={{fontSize:"12px",color:"#6b7280",lineHeight:"1.5"}}>{desc}</div>
      </div>
    ))}
  </div>
</div>

{/* ─── QUICK START ─────────────────────────────────────────────────────── */}
<div style={{marginBottom:"56px"}}>
  <h2 style={{fontSize:"24px",fontWeight:"800",color:"#111827",margin:"0 0 24px",letterSpacing:"-0.5px"}}>
    Quick start
  </h2>
  <div style={{display:"flex",flexDirection:"column",gap:"12px"}}>
    {[
      {
        step:"1",title:"Registrar el componente",
        code:`// Importar en el entry point de tu app
import 'josehollow-design-system/components/button';
import 'josehollow-design-system/components/icon';`
      },
      {
        step:"2",title:"Usar en HTML / JSX",
        code:`<!-- HTML nativo -->
<dsh-button variant="primary" color="blue">Hola mundo</dsh-button>

// React 19 — soporte nativo de custom elements
<dsh-button variant="secondary" onclick={e => console.log(e)}>
  Secundario
</dsh-button>`
      },
      {
        step:"3",title:"Escuchar eventos",
        code:`// JS nativo
document.querySelector('dsh-button')
  .addEventListener('onClick', e => console.log(e));

// React 18 — useRef + addEventListener
const ref = useRef(null);
useEffect(() => {
  ref.current?.addEventListener('onClick', handler);
}, []);`
      },
    ].map(({step,title,code}) => (
      <div key={step} style={{borderRadius:"14px",overflow:"hidden",border:"1px solid #1e3a4a"}}>
        <div style={{background:"#0c2333",padding:"12px 20px",display:"flex",alignItems:"center",gap:"12px",borderBottom:"1px solid #1e3a4a"}}>
          <div style={{background:"#0076a9",color:"#ffffff",borderRadius:"50%",width:"22px",height:"22px",display:"inline-flex",alignItems:"center",justifyContent:"center",fontSize:"11px",fontWeight:"800",flexShrink:0}}>{step}</div>
          <span style={{color:"#f1f5f9",fontSize:"13px",fontWeight:"600"}}>{title}</span>
        </div>
        <pre style={{margin:0,padding:"20px 24px",fontSize:"13px",color:"#7dd3fc",lineHeight:"1.8",overflowX:"auto",background:"#0f172a",fontFamily:"'Fira Code','Cascadia Code',monospace",whiteSpace:"pre"}}>{code}</pre>
      </div>
    ))}
  </div>
</div>

{/* ─── PALETA ──────────────────────────────────────────────────────────── */}
<div style={{marginBottom:"56px"}}>
  <h2 style={{fontSize:"24px",fontWeight:"800",color:"#111827",margin:"0 0 6px",letterSpacing:"-0.5px"}}>
    Paleta de colores
  </h2>
  <div style={{fontSize:"15px",color:"#6b7280",margin:"0 0 28px"}}>
    Tokens con prefijo <code style={{background:"#f3f4f6",padding:"2px 7px",borderRadius:"5px",fontSize:"13px",color:"#0076a9"}}>Dsh</code> disponibles en JS y CSS custom properties.
  </div>
  <div style={{display:"flex",flexDirection:"column",gap:"20px"}}>
    {[
      {label:"Primary C — Azul corporativo",colors:[
        {name:"C0",hex:"#00648f"},{name:"C1",hex:"#0076a9"},{name:"C2",hex:"#008ec4"},
        {name:"C3",hex:"#1896d3"},{name:"C4",hex:"#4ebce3"},{name:"C5",hex:"#92d5f1"},{name:"C6",hex:"#def4ff"},
      ]},
      {label:"Primary A — Ámbar / Acento",colors:[
        {name:"A0",hex:"#8c5d00"},{name:"A1",hex:"#b37400"},{name:"A2",hex:"#d59608"},
        {name:"A3",hex:"#f9be00"},{name:"A4",hex:"#fad271"},{name:"A5",hex:"#fde1a7"},{name:"A6",hex:"#fff3de"},
      ]},
      {label:"Secondary G — Grises",colors:[
        {name:"G1",hex:"#3e4545"},{name:"G2",hex:"#586264"},{name:"G3",hex:"#a8b3b7"},
        {name:"G4",hex:"#c9d0d3"},{name:"G5",hex:"#dfe3e5"},{name:"G6",hex:"#f6f7f7"},
      ]},
    ].map(({label,colors}) => (
      <div key={label}>
        <div style={{fontSize:"11px",fontWeight:"700",color:"#374151",marginBottom:"10px",textTransform:"uppercase",letterSpacing:"0.8px"}}>{label}</div>
        <div style={{display:"flex",gap:"6px",flexWrap:"wrap"}}>
          {colors.map(({name,hex}) => {
            const isDark = ["#00648f","#0076a9","#008ec4","#1896d3","#8c5d00","#b37400","#d59608","#3e4545","#586264"].includes(hex);
            return (
              <div key={name} style={{textAlign:"center",width:"76px"}}>
                <div style={{height:"44px",borderRadius:"10px",background:hex,border:"1px solid rgba(0,0,0,0.07)",marginBottom:"6px",display:"flex",alignItems:"center",justifyContent:"center"}}>
                  <span style={{fontSize:"9px",fontWeight:"700",color:isDark?"rgba(255,255,255,0.8)":"rgba(0,0,0,0.5)",letterSpacing:"0.3px"}}>{name}</span>
                </div>
                <div style={{fontSize:"10px",color:"#9ca3af",fontFamily:"monospace"}}>{hex}</div>
              </div>
            );
          })}
        </div>
      </div>
    ))}
  </div>
</div>

{/* ─── AUTOR ───────────────────────────────────────────────────────────── */}
<div style={{borderTop:"1px solid #e5e7eb",paddingTop:"48px",display:"flex",gap:"28px",alignItems:"flex-start",flexWrap:"wrap"}}>
  <img
    src="https://github.com/JoseHollow24.png"
    alt="José Aguilar"
    style={{width:"88px",height:"88px",borderRadius:"50%",border:"3px solid #e5e7eb",objectFit:"cover",flexShrink:0}}
  />
  <div style={{flex:1,minWidth:"240px"}}>
    <div style={{fontSize:"11px",fontWeight:"700",color:"#0076a9",textTransform:"uppercase",letterSpacing:"1.5px",marginBottom:"6px"}}>
      Creado por
    </div>
    <div style={{fontSize:"22px",fontWeight:"800",color:"#111827",marginBottom:"10px",letterSpacing:"-0.5px"}}>
      José de Jesús Aguilar Pérez
    </div>
    <div style={{fontSize:"15px",color:"#4b5563",lineHeight:"1.7",marginBottom:"24px",maxWidth:"520px"}}>
      Desarrollador Frontend especializado en sistemas de diseño, Web Components y arquitecturas de UI modernas.
      Este design system es un proyecto personal construido sobre <strong>AtomicoJS</strong> para explorar
      componentes reutilizables, encapsulados y accesibles sin dependencias de framework.
    </div>
    <div style={{display:"flex",gap:"12px",flexWrap:"wrap",alignItems:"center"}}>
      <dsh-button variant="primary" color="blue" href="https://github.com/JoseHollow24" target="_blank">
        GitHub
      </dsh-button>
      <dsh-button variant="secondary" color="blue" href="https://www.linkedin.com/in/jose-de-jesus-aguilar-p%C3%A9rez-b0b20113a/" target="_blank">
        LinkedIn
      </dsh-button>
    </div>
  </div>
</div>

{/* ─── FOOTER ──────────────────────────────────────────────────────────── */}
<div style={{marginTop:"48px",paddingTop:"20px",borderTop:"1px solid #f3f4f6",display:"flex",justifyContent:"space-between",alignItems:"center",flexWrap:"wrap",gap:"8px"}}>
  <span style={{fontSize:"13px",color:"#6b7280",fontWeight:"500"}}>DSH Design System — v1.0.0 alfa</span>
  <span style={{fontSize:"13px",color:"#6b7280"}}>AtomicoJS · Storybook · Vite · Plus Jakarta Sans</span>
</div>

</div>
